# URL do backend FastAPI (Render). Em dev pode deixar comentado para usar http://localhost:8000.
# Na Vercel, defina esta variável apontando para a URL pública do seu backend no Render.
VITE_API_URL=https://SEU-BACKEND.onrender.com
